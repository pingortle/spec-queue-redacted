{
  "name": "server",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "TEST_BROWSER_DRIVER=nightmare meteor test --once --driver-package dispatch:mocha --port 3030",
    "watch-tests": "meteor test --port 3030 --driver-package practicalmeteor:mocha"
  },
  "dependencies": {
    "JSONPath": "^0.11.2",
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "faker": "^4.1.0",
    "nightmare": "^2.10.0",
    "sinon": "^2.3.5",
    "sinon-chai": "^2.11.0"
  }
}
